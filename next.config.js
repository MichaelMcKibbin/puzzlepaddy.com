// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // important for `next export`
    // basePath: '',    // only if you ever host under a subfolder
};

module.exports = nextConfig;
